# IsekaiAuctions - Verschuls
# If you have any question or suggestion please contact to me.
# If you are using this plugin, you are accepting Terms of Use.
#
# Website: https://verschuls.xyz
# Discord Server: https://discord.gg/XbpJHXfMXu

# PLUGIN SETTINGS
settings:
  enable_plugin: true # Plugin can be enabled and disabled
  enable_debug: false # Useful for debugging database and some systems.
  enable_log: false # Internal logging system, useful for finding exploits and bugs
  enable_main_menu: true
  input_type: "sign" # Default input, types: sign, anvil or chat
  # Uncategorized items will be found in this category
  # All items can be sold with this setting
  return_category:
    enabled: true
    category: "miscellaneous"
  # This menu will be opened when the /ah command used with this setting.
  # Types: auctions, main
  # Delete or make it empty to disable this setting.
  menu_to_open_directly: ""
  add_time_when_bid: 10 # SECONDS. When new bid is created, time is automatically added to the auction.
  purchase_cooldown: 60 # SECONDS. Once the auction is created, people will be able to purchase it after this period.
  bid_cooldown: 60 # SECONDS. Once the auction is created, people will be able to bid it after this period.
  default_economy: vault_economy
  default_type: "BIN"
  default_rarity: "all" # Default auction rarity for sorting.
  default_price: 1000 # Default auction creation price.
  default_duration: 21600 # Default auction creation duration.
  default_category: "weapons" # Default category that is selected when opening /ah menu
  default_sort_type: "HIGHEST_PRICE" # Default sort that is selected when opening /ah menu
  default_filter_type: "ALL" # Default filter that is selected when opening /ah menu
  purge_auctions: 60 # DAYS. Auctions older than this day will be automatically deleted. Recommended to keep the database clean.
  bid_formula: "%highest_bid% + %highest_bid% / 10" # You can set the minimum amount more than the previous bid for a new bid.
  duration_fee: # Duration fee can be customizable
    enabled: true
    minimum_fee: 50 # Duration fee can't be lower than this setting
    formula: "%hours% * 50" # Current Placeholders: %hours%
  auction_flags: # Auctions' example items will have these flags
    - HIDE_ATTRIBUTES
  anti_lag: # With this system, you can prevent dupe problems a bit.
    player:
      enabled: false
      maximum_ping: 250
    server:
      enabled: false
      minimum_tps: 15
      seconds: 5 # TPS will be calculated every this seconds
  disabled_worlds: # Auction system can be disabled on some worlds
    enabled: false
    world_list:
      - world_the_end
      - world_nether


# DATABASE STORAGE
# Remote databases (require connection settings below):
#   - MySQL, MariaDB (preferred), PostgreSQL
# Local database (no extra config needed):
#   - H2 (default)
storage_method: h2
# REMOTE DATABASE SETTINGS (skip if using H2)
# Connection details shared between MySQL, MariaDB, PostgreSQL
database:
  address: "" # e.g. 0.0.0.0:3306
  database: ""
  username: ""
  password: ""

  # DEFAULT VALUES WORK WELL FOR MOST SERVERS - Only adjust if needed
  # ⚠️ For most users: Only adjust maximum_pool_size (for player count) and minimum_idle (for performance)
  pool_settings:
    # Max connections to DB (increase for 50+ players)
    maximum_pool_size: 20
    # Min ready connections (set to 10-15 for better performance)
    minimum_idle: 10
    # Connection refresh interval - 30 min (keep default)
    maximum_lifetime: 1800000
    # Wait time for connection - 5 sec (keep default)
    connection_timeout: 5000
    # Remove unused connections after 15 min (keeps pool efficient)
    idle_timeout: 600000


# REDIS MULTISERVER SYNC
# Enables real-time auction synchronization across multiple servers
# Redis acts as a message broker to instantly share auction data between servers
# When enabled, all auction events (create/bid/sell) are broadcast to all connected servers
redis:
  enabled: false              # Enable multiserver auction sync
  channel: "isekaiauctions"   # Redis pub/sub channel name
  host: ""                    # Redis server IP/hostname
  port: 6379                  # Redis server port (default: 6379)
  user: ""                    # Redis user auth (leave empty if none)
  password: ""                # Redis auth password (leave empty if none)

# AUCTION MESSAGE BROADCASTS
# With this system, messages can be clickable.
# Messages can be found in the messages.yml file.
broadcast_messages:
  enabled: true
  check_if_muted: false # Broadcasts will not be sent if the player is muted. (Currently AdvancedBans and LiteBans supported)
  # Commands can be executed along with the broadcasts.
  # Examples:
  # [player] /ah open (Player will execute this command.)
  # [console] /ahadmin open %player_name% (Console will execute this command.)
  commands: []
  # Text Types: COPY_TO_CLIPBOARD, RUN_COMMAND, SUGGEST_COMMAND, OPEN_URL, OPEN_FILE, CHANGE_PAGE
  # Current Placeholders: %auction_uuid%
  normal_auction_broadcast:
    type: SUGGEST_COMMAND
    text: "/ah view %auction_uuid%"
    hover: "&fClick to get view auction command!"
  bin_auction_broadcast:
    type: SUGGEST_COMMAND
    text: "/ah view %auction_uuid%"
    hover: "&fClick to get view auction command!"
  new_bid:
    type: SUGGEST_COMMAND
    text: "/ah view %auction_uuid%"
    hover: "&fClick to get view auction command!"
  outbid:
    type: SUGGEST_COMMAND
    text: "/ah view %auction_uuid%"
    hover: "&fClick to get view auction command!"
  sold:
    type: SUGGEST_COMMAND
    text: "/ah view %auction_uuid%"
    hover: "&fClick to get view auction command!"
  auction_view_info:
    type: SUGGEST_COMMAND
    text: "/ah view %auction_uuid%"
    hover: "&fClick to get view auction command!"

# BIN AUCTION SETTINGS
bin_auction:
  enabled: true # BIN auctions can be enabled or disabled.
  fee: true # enable or disable fee
  # Fee percentages can be editable within certain price ranges.
  # Default Configuration:
  # 0 to 500,000: 1%
  # 500,000 to 1,000,000: 2.5%
  # 1,000,000 and higher: 5%
  price_fees: # PERCENT.
    0: 1
    500000: 2.5
    1000000: 5

# NORMAL AUCTION SETTINGS
normal_auction:
  enabled: true # NORMAL auctions can be enabled or disabled.
  fee: true # enable or disable fee
  # Fee percentages can be editable within certain price ranges.
  # Default Configuration:
  # 0 and higher: 1%
  price_fees: # PERCENT.
    0: 1

# AUCTION LIMITS
player_limits:
  auction_limit:
    default: 9
    permissions:
      - isekaiauctions.vip:18
      - isekaiauctions.mvip:27
  duration_limit:
    default: 604800
    permissions:
      - isekaiauctions.vip:907200
      - isekaiauctions.mvip:1209600
  price_limit:
    default: 250000000
    permissions:
      - isekaiauctions.vip:500000000
      - isekaiauctions.mvip:100000000
  bid_limit:
    default: 9
    permissions:
      - isekaiauctions.vip:500000000
      - isekaiauctions.mvip:100000000

# PLUGIN ADDONS
addons:
  discord: # Enable this to use the discord webhook system for sending useful messages.
    enabled: false
    webhook_url: ""
    create_auction:
      enabled: true
      message: ":small_blue_diamond:__%player_name%__ created **%auction_type% AUCTION** for **%item_name%**!"
    cancel_auction:
      enabled: true
      message: ":small_blue_diamond:__%player_name%__ cancelled **%auction_type% AUCTION** for **%item_name%**!"
    bought_item:
      enabled: true
      message: ":small_blue_diamond:__%player_name%__ bought **%item_name%** for **%auction_price% coins** from **%seller_name%**!"
    sold_item:
      enabled: true
      message: ":small_orange_diamond:__%seller_name%__ sold **%item_name%** for **%auction_price% coins** to **%player_name%**!"
    bid_item:
      enabled: true
      message: ":small_blue_diamond:__%player_name%__ bid **%bid_price% coins** for **%item_name%** in **%seller_name%**'s auction!"

# NUMBER FORMAT SETTINGS
number_format:
  enabled: true # Enable or disable number formatting
  type: "decimal" # Number format type, types: decimal, short
  decimal_settings: # Example: 10.2
    minimum_fraction: 0
    maximum_fraction: 1
  short_settings: # Examples: 10k, 25M, 1T
    thousand: "k"
    million: "M"
    billion: "G"
    trillion: "T"
    quadrillion: "P"
    quintillion: "E"
    sextillion: "Z"
    septillion: "Y"
    octillion: "R"
    nonillion: "Q"
    decillion: "Dc"
    undecillion: "Ud"
    duodecillion: "Dd"
    tredecillion: "Td"
    quattuordecillion: "Qd"

# TIME FORMAT SETTINGS
time_format:
  # These texts are used to indicate the duration in the /ah sell command.
  # Example: /ah sell [type] [price] [1w 3days 5minutes/2weeks 3m 5seconds]
  convert_times:
    months:
      - mo
      - month
      - months
    weeks:
      - w
      - week
      - weeks
    days:
      - d
      - day
      - days
    hours:
      - h
      - hour
      - hours
    minutes:
      - m
      - minute
      - minutes
    seconds:
      - s
      - second
      - seconds
  auction_times:
    months: "mo"
    weeks: "w"
    days: "d"
    hours: "h"
    minutes: "m"
    seconds: "s"
    ending: "ENDING"
  other_times:
    months: " months"
    weeks: " weeks"
    days: " days"
    hours: " hours"
    minutes: " minutes"
    seconds: " seconds"
    ending: "ENDING"

# MENU SOUNDS
sounds:
  inventory_item_click: # When player clicks an item in his own inventory to select for the auctions, the plugin will play this sound.
    enabled: false
    name: "CLICK"
    volume: 1
    pitch: 2
  auction_item_click: # When player clicks to auction item in auctions menu, the plugin will play this sound.
    enabled: false
    name: "CLICK"
    volume: 1
    pitch: 2
  category_item_click: # When the player clicks the auction category item, the plugin will play this sound.
    enabled: false
    name: "CLICK"
    volume: 1
    pitch: 1.5
  filter_item_click: # When player switches filters, the plugin will play this sound.
    enabled: false
    name: "CLICK"
    volume: 1
    pitch: 1
  sorter_item_click: # When player switches sorters, the plugin will play this sound.
    enabled: false
    name: "CLICK"
    volume: 1
    pitch: 1
  bought_auction:
    enabled: false
    name: "ORB_PICKUP"
    volume: 1
    pitch: 0.6
  bid_auction:
    enabled: false
    name: "ORB_PICKUP"
    volume: 1
    pitch: 0.6
  cancel_auction:
    enabled: false
    name: "ORB_PICKUP"
    volume: 1
    pitch: 0.6
  buyer_collect_auction:
    enabled: false
    name: "ORB_PICKUP"
    volume: 1
    pitch: 0.6
  seller_collect_auction:
    enabled: false
    name: "ORB_PICKUP"
    volume: 1
    pitch: 0.6
  sold_auction:
    enabled: false
    name: "ORB_PICKUP"
    volume: 1
    pitch: 0.6
  not_enough_money:
    enabled: false
    name: "ORB_PICKUP"
    volume: 1
    pitch: 0.6

# DON'T CHANGE THIS MANUALLY
file_versions: 1.3